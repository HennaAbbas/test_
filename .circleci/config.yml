# Orb 'circleci/sumologic@1.0.6' resolved to 'circleci/sumologic@1.0.6'
version: 2.1



jobs:
  build:
    docker:
      - image: cimg/node:12.18
    steps:
      - checkout    
      - run: whoami
      - run:
          command: |
            set -ex
            automation_path="$HOME/automation"   
  test:
    docker:
      - image: cimg/node:12.18
    steps:
      - checkout    
      - run: whoami
      - run:
          command: |
            set -ex
            automation_path="$HOME/automation"             
            
# # #
workflows:
    build_and_push:
      jobs:
        - build
        - test: 
            name: old-joe-test-windows-smoke-win/default

        

      
      

