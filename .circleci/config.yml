version: 2.1
executors:
  xcode-executor:
    macos:
      xcode: "16.0.0"
jobs:
  build:
    executor: xcode-executor
    steps:
      - checkout
      - run:
          name: Print Environment Variables
          command: env
workflows:
  version: 2
  build:
    jobs:
      - build
