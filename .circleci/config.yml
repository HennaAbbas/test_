# Orb 'circleci/sumologic@1.0.6' resolved to 'circleci/sumologic@1.0.6'
version: 2.1

orbs:
  shared: indigo/shared@volatile
  python: indigo/python@dev:55f1fbb2250aebead996e822bbdee6dd89a17427

jobs:
  build:
    docker:
      - image: cimg/node:12.18
    steps:
      - checkout    
      - run: whoami
      - run:
          command: |
            set -ex
            automation_path="$HOME/automation"   
            
# # #
workflows:
    build_and_push:
      jobs:
        - sumologic/workflow-collector
        - build

      
      

