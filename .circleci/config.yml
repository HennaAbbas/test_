version: 2.1

jobs:
  build:
    docker:
      - image: cimg/openjdk:11.0
    steps:
      - checkout
      - run: pwd
      - run: source ${CIRCLE_WORKING_DIRECTORY}/script.sh
      - run: echo ${CIRCLE_WORKING_DIRECTORY}/script.sh
      - run:
          name: PWD
          command: |
            cd /home
            ls -al
            mkdir -p /Users/distiller/project/home/circleci

workflows:
  workflow-mac:
    jobs:
      - build
