version: 2.1
orbs: 
  azure-acr: circleci/azure-acr@0.2.0


jobs:
   build: 
    machine: true
    steps:
    - azure-acr/build-and-push-image:    
        login-server-name: "helloworld"
        registry-name: "helloworld"
        repo: "helloworld"

   


workflows:
  my_workflow:
    jobs:
      - build
          
  
