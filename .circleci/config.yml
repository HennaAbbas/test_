version: 2     
jobs:
  build:
    docker: 
      - image: cimg/node:17.0.1-browser
    steps:
      - checkout 
         


version: 2.1
workflows:
  test_and_build:
    jobs:
      - build
          

      #test
#test
