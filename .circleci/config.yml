version: 2.1
jobs:
   build:
    parameters:   
      add_suffix:
        type: boolean
        default: true        
    machine:
      image: ubuntu-2004-cuda-11.2:202103-01
    steps:
    - checkout   
    - when:
        condition: << parameters.use_remote_execution >>
        steps:
          - run: echo "use remote"         


workflows:
  my_workflow:
    jobs:
      - build
      - build: 
          name: other build       

  other_workflow:
    jobs:
      - build         
      - build: 
          name: other build 
          add_suffix: false
    
