version: 2.1

jobs:
  build:
    machine: true
    steps:
      - checkout
      - run: pwd
      - run: source ${CIRCLE_WORKING_DIRECTORY}/script.sh
      - run:
          name: PWD
          command: |
            cd /home
            ls -al
            mkdir -p /Users/distiller/project/home/circleci

workflows:
  workflow-mac:
    jobs:
      - build
