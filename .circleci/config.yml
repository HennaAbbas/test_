version: 2.1

jobs:
  build:
    docker:
     - image: cimg/node:14.16-browsers
    steps:
      - run: echo "hello world"
          
          
  test:
    parameters:   
      add_suffix:
        type: boolean
        default: true        
    machine:
      image: ubuntu-2004-cuda-11.2:202103-01
    steps:
      - checkout


workflows:
  my_workflow:
    jobs:
      - build
      - test

