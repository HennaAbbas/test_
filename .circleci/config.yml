version: 2.1



jobs:
  build:
    docker:
      - image: cimg/android:2021.9.1-ndk
      
    steps:

      - run : printenv    
            
        

  test:
    docker:
      - image: circleci/python:3.8.12-node-browsers
      
    steps:
      - run : echo "hello world - test job"        
    
            
# # #
workflows:
    build_and_push:
      jobs:
        - build   

    my-workflow:
      when:
        and:
          - not:
              matches:
                pattern: "^master$"
                value: << pipeline.git.branch >>        
      jobs:
  
        - test                






