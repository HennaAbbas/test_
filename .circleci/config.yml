version: 2.1
jobs:
  build:
    docker:
      - image: cimg/ruby:3.1.3
    steps:
      - checkout
      - run: echo "hello world!"
      - run: lsb_release -a

      
workflows:
    build_and_push:
      jobs:
        - build
            
