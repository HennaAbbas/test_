version: 2.1
#test
jobs:
  build:
    machine:
      image: ubuntu-2604:current
    resource_class: medium  

    steps:
      - checkout
      - run:
          name: Build
          command: echo "Building..."


workflows:
  build:
    jobs:
      - build
  hold:     
      - hold-for-approval:
          type: approval

