version: 2.1
jobs:
  build:
    docker:
      - image: cimg/node:10.18.1
    steps:
      - run: echo $NVM_DIR
      - run: echo "hello world!"
      - run: node --version

  
      
workflows:
    build_and_push:
      jobs:
        - build
#test
