version: 2.1

orbs:
  mac: circleci/macos@2.3.0
#test

jobs:            
  build:
    macos:
      xcode: 12.0.0
    resource_class: macos.x86.medium.gen2  
    steps:
      - mac/enable-vnc   
      
workflows:
  all:
    jobs:
      - build
      
      
