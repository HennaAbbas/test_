# version: 2
jobs:
  build:
    docker:
      - image: cimg/python:3.10.2
    steps:
      - checkout
      - run: python --version 
      - run: sudo apt-get python 2.7
      - run: python --version       

        
      
workflows:
  version: 2
  build:
    jobs:
      - build


