version: 2
jobs:
  build:
    docker:
      - image: circleci/node
    steps:
      - checkout
      - run: echo "hello world"
      - run: mkdir tests
      - run: cd tests && touch my_test_1.txt
      - run: echo "this is a test" > my_test_1.txt
      - store_artifacts:
          path: tests
          

      

workflows:
    version: 2
    build_and_push:
      jobs:
        - build
                
                
                #test

