version: 2.1

jobs:
  build:
    machine:
      image: ubuntu-2604:current
    resource_class: medium  

    steps:
      - checkout
      - run: wrq


workflows:
  build:
    jobs:
      - build    

