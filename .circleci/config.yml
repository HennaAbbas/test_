version: 2.1
jobs:
  my_long/test_name_with_slash:
    machine:
      image: ubuntu-2004:202107-02
      docker_layer_caching: false
    resource_class: xlarge
    steps:
      - run: |
          lscpu
          lscpu | egrep 'Model name|Socket|Thread|NUMA|CPU\(s\)'
          lscpu -p
      - run: wqr
    

workflows:
  build-workflow:
    jobs:
      - my_long/test_name_with_slash: 
          post-steps:
            - run:
                command: ls /home/circleci/project/lib | grep LogsToElasticsearch 
                when: always          

#test
#test
