version: 2.1


# Define the jobs we want to run for this project
jobs:
  build:
    docker:
      - image: cimg/node:20.10.0
    resource_class: xlarge  
    steps:
      - checkout
      - run : Echo $test_var
      - run: yarn cache clean && yarn install --verbose
      - run : yarn add sharp@0.33.5 

# Orchestrate our job run sequence
workflows:
  build_and_test:
    jobs:
      - build


#test
