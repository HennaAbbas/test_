version: 2.1

jobs:

  build:
    machine:
      image: ubuntu-2004:202111-01
    steps:
#       - run: circleci update check
#       - run: circleci step halt
      - run:
          name: Check 
          command: |
            if [[ "$CIRCLE_NODE_INDEX" ]]
            then
                echo "run this step"
                echo "step halt!" && circleci step halt 
                echo "the end."
            fi      
workflows:
  build-workflow:
    jobs:
      - build


