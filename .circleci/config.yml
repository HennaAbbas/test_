version: 2.1

orbs:
  win: circleci/windows@5.0

jobs:
  build:
    executor: 
      name: win/server-2019
      version: edge
    steps:
      - run:
          name: List Chocolatey packages
          command: choco list --local-only
      - run:
          name: List Chocolatey packages
          command: choco list --local-only > 
      - run: cat choco-packages.txt    
          
    
    
    
    

