

version: '2.1'

jobs:
  build:
    docker:
    - image: cimg/node:16.11-browsers

    steps:
      - checkout
#       - run: sudo apt-get install -y ca-certificates
      - restore_cache:
          keys:
          - node-modules-cache-node-gallium-npm-8-{{ .Environment.CACHE_VERSION }}-{{ checksum "package-lock.json" }}
          - node-modules-cache-node-gallium-npm-8-{{ .Environment.CACHE_VERSION }}-


workflows:
  main:
    jobs:
      - build


