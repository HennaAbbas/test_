version: 2.1
jobs:
  release:
    executor: linux
    parameters:
      target_arch:
        type: string
        default: "hello"
#       target_os:
#         type: string
    steps:
    - run: echo << parameters.target_arch >>
workflows:
  my-workflow:
    jobs:
      - release:  
          target_arch: "hiii"


