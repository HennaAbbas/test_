version: 2.1

orbs:
  python: circleci/python@2.1.1

jobs:
  noop:
    docker:
      - image: cimg/base:current
    steps:
      - run: echo "done"

workflows:
  build-and-test:
    jobs:
      - noop
      - python/test:
          requires:
            - noop
          pkg-manager: pip
      - python/test:
          requires:
            - noop
          pkg-manager: pipenv
