version: 2.1

workflows:
  build-workflow:
    jobs:
  nightly:
    triggers:
      - schedule:
          cron: "0 0 * * *"

    jobs:
      - runner
jobs:
  runner:
    machine: true

    steps:
      - run: echo "Hi I'm on machine-runner!"
