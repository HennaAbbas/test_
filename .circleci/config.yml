
   
version: 2.1

jobs:
  build:
    docker:
      - image: cimg/python:3.9

    steps:
      - run : python3 -m pip install coverage
      - run : echo $PATH           

            
# # #
workflows:
    build_and_push:
      jobs:
        - build   
       