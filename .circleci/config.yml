version: 2.1

orbs:
  node: circleci/node@5.2.0

jobs:
  validate:
    docker:
      - image: cimg/node:20.11
    resource_class: medium
    steps:
      - run:
          name: env
          command: env

workflows:
  ci:
    jobs:
      - validate:
          context: ongoody-frontend-staging




