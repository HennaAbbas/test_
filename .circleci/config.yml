workflows:
  workflow:
    jobs:
      - build
      - hold:
          type: approval
          requires:
            - build
          filters:
            branches:
              only:
                - burger
      - deploy:
          requires:
            - hold
          filters:
            branches:
              only:
                - burger
