version: 2
executorType: docker
containerInfo:
  - image: circleci/ruby:2.5.1-node
    env:
      - RAILS_ENV=test
      - RACK_ENV=test

jobs:
  deploy:

    resource_class: small

    steps:
      - checkout


workflows:
  version: 2
  my_workflow:
    jobs:
        - deploy
       

      


#test 

