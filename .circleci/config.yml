version: 2.1
orbs:
  win: circleci/windows@2.4.1
jobs:
  # run collections tests in a separate serial job to make serial workflows more granular
  build:
    executor:
      name: win/default
      version: "edge"
      size: "medium"
    steps:
      - run: py --list
      - run: py -3.9
      - run:
          name: Check installed python verion
          command: python --version
      - run:
          name: Check all installed python verions
          command: choco install python --version=3.9.0 
      - run: py -3.9          
      - run:
          name: Check installed python verion
          command: python --version     
workflows:
  main:
    jobs:
      - build
          
