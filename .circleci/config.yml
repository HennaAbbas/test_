orbs:
  terraform: circleci/terraform@3.2.0
jobs:
  single-job-lifecycle:
    executor: terraform/default
    steps:
      - checkout
      - terraform/install:
          arch: amd64
          os: linux
          terraform_version: 1.3.4
      # - terraform/fmt:
      #     path: .
      # - terraform/validate:
      #     path: .
      # - terraform/init:
      #     backend: true
      #     backend_config_file: ./init-tfvars/prd.tfvars
      #     path: .
      # - terraform/plan:
      #     backend_config_file: ./init-tfvars/prd.tfvars
      #     path: .
    working_directory: ~/src
workflows:
  single-job-lifecycle:
    jobs:
      - single-job-lifecycle

