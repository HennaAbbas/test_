jobs:
  test-coordinator:
    type: no-op

  test-unit:
    docker:
      - image: cimg/node:20.10
    steps:
      - checkout
      - run: npm run test:unit


workflows:
  build-test-deploy:
    jobs:
      - test-coordinator
      - test-unit
      
