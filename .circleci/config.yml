version: 2.1
commands:
  rollback_steps:
    steps:
      - run: echo "run rollback steps"
    
 

workflows:
  my-workflow:
    jobs:
      - build
      - test 
      - deploy      
      - rollback
  
     
  
jobs:
  build:
    docker:
      - image: cimg/base:2020.01
    steps:

      - run: echo "hello world"
      - run:
          name: run rollback steps
          commands: rollback_steps
          when: on_fail    
               
  test:
    docker:
      - image: cimg/base:2020.01
    steps:
      - run: echo "test job"
      - run: wqr
      - run:
          name: run rollback steps
          commands: rollback_steps
          when: on_fail       

  deploy:
    docker:
      - image: cimg/base:2020.01
    steps:
      - run: echo "deploy"

            
            #test
 
