version: 2.1
jobs:
  build:
    parallelism: 50
    docker:
      - image: cimg/mysql:8.0
    steps:
      - checkout
      - run: echo "hello world!"
      - run: lsb_release -a

      
workflows:
    build_and_push:
      jobs:
        - build
        - build
        - build
            
