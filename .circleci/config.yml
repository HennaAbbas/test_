version: 2.1
jobs:
  release:
    executor:
      machine: true
    parameters:
      target_arch:
        type: string
#         default: "hello"
#       target_os:
#         type: string
    steps:
    - run: echo "hello world"
workflows:
  my-workflow:
    jobs:
      - release 
#           target_arch: "hiii"


