version: 2.1
jobs:
  build:
    docker:
      - image: cimg/node:12.18
    steps:
      - checkout
      - run: echo "hello world!"
      - run: lsb_release -a

      
workflows:
    build_and_push:
      jobs:
        - build
            
