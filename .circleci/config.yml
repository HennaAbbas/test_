version: 2.1

orbs:
  node: circleci/node@5

workflows:
  test:
    jobs:
      - node/run:
          name: run-1
          npm-run: test
      - node/run:
          name: run-2
          npm-run: build
