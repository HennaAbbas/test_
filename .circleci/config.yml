version: 2.1

jobs:
  build:
    docker:
     - image: cimg/node:14.16-browsers
    steps:

      - run:
          name: Update PATH and Define Environment Variable at Runtime
          command: |
            eecho "NX_MAIN_BRANCH: $CIRCLE_BRANCH"
            echo "export NX_MAIN_BRANCH='$CIRCLE_BRANCH'" >>$BASH_ENV  
            
      - run: echo $NX_MAIN_BRANCH            

workflows:
  main:
    jobs:
      - build
          
