version: '2.1'
orbs:
  commitlint: conventional-changelog/commitlint@1.0
workflows:
  my-workflow:
    jobs:
      - run: npm --version
      - run : sudo npm install -g npm@10.5.0
      - commitlint/lint
#test
