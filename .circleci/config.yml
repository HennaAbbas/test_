version: '2.1'
orbs:
  win: circleci/windows@4.1
jobs:
  build:
    executor: win/server-2019:edge
    steps:
      - run: python --version
workflows:
  my-workflow:
    jobs:
      - build

          
