version: 2.1
orbs: 
  argo_deploy: stint/argo_deploy@0.0.1
jobs:

  build:
    docker:
      - image: ubuntu-2004:202111-01
    steps:
      - run: circleci update check
      - run: circleci step halt
workflows:
  build-workflow:
    jobs:
      - build
      - argo_deploy/deploy
      - docker-build-push

