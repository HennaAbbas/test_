version: 2.1
orbs:
  win: circleci/windows@2.4.1
jobs:
  install_python_3
    executor:
      name: win/default
    steps:
      - run: choco install python --version=3.9.0
      - run: python --version        
   
workflows:
  main:
    jobs:
      - build

