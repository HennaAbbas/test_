version: 2.1
workflows:
  testing:
    jobs:
      - runner-test
jobs:
  runner-test:
    machine: true
    resource_class: mikhailtymchukft/engineering-default
    steps:
      - run: echo "I'm on runner!"
