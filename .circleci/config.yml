version: 2.1
# commands:
#   list-files:
#     parameters:
#       all:
#         description: include all files
#         type: boolean
#         default: false
#       short:
#         description: Keep list of files short
#         type: boolean
#         default: N
#     steps:
#       - run: echo "hello world!"
jobs:
  build:
    docker:
      - image: cimg/node:12.18
    steps:
      - checkout
      - run: echo "hello world!"
      - run: echo $password  
      - run: echo $env_var 
      - run: 
          command:
            if [ $env_var = "password/" ]; then
              echo "hello"
            fi
#             if[ -n "$env_var" ]; 
# #             if[ "$1" -eq "Server" ];
#             then
#                 echo "Set"
#             else
#                 echo "Not set"
#             fi 
  
      
workflows:
    build_and_push:
      jobs:
        - build:
            context:
              - my-context
