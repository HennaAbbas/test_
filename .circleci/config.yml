version: 2.1

jobs:
  test-coordinator:
    docker:
      - image: cimg/base:stable
    steps:
      - run: echo "Coordinator job - no operation needed"
  
  test-unit:
    docker:
      - image: cimg/node:20.10
    steps:
      - checkout
      - run: npm run test:unit

workflows:
  build-test-deploy:
    jobs:
      - test-coordinator
      - test-unit
