version: 2.1

workflows:
  my-workflow:
    jobs:
      - build
      - test 
      - deploy      
      - rollback
  
     
  
jobs:
  build:
    docker:
      - image: cimg/base:2020.01
    steps:

      - run: echo "hello world"
     
          
  test:
    docker:
      - image: cimg/base:2020.01
    steps:
      - run: echo "test job"
      - run: wqr
      - run: 
          name: run rollback job
          command: 
                  curl -u ${CIRCLECI_TOKEN}: -X POST --header "Content-Type: application/json" -d '{
                  "parameters": {
                    "run_job": true
                  }
                }' https://circleci.com/api/v2/project/gh/HennaAbbas/test_/pipeline


          when: on_fail
  deploy:
    docker:
      - image: cimg/base:2020.01
    steps:
      - run: echo "deploy"
  rollback:
    parameters: 
      run_job: 
        default: false
        type: boolean
    docker:
      - image: cimg/base:2020.01
    steps:
      - run : echo " rollback job pre-step"
      - when: 
          condition: << parameters.run_job >>
          steps:
            - run: echo "running rollback job"
            
            #test
 
