version: 2.1

orbs:
  win: circleci/windows@2.2.0
  nx: nrwl/nx@1.2.2
  

jobs:
  build:
    docker:
     - image: cimg/node:14.16-browsers
    steps:
      - run: echo "hello world"
          
               

workflows:
  main:
    jobs:
      - build

  nightly:
    triggers:
      - schedule:
          #every weekday at 6am
          cron: "* * * * 1-5"
          filters:
            branches:
              only:
                - master
                
    jobs:
      - build
          
