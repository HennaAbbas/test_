

version: '2.1'

jobs:
  build:
    resource_class: medium
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - setup_remote_docker:
          docker_layer_caching: true
      - run: docker build ./


workflows:
  main:
    jobs:
      - build


