version: 2.1

jobs:
  test-coordinator:
    type: no-op
    docker:
      - image: cimg/node:20.10
    steps:
      - checkout  

  test-unit:
    docker:
      - image: cimg/node:20.10
    steps:
      - checkout


workflows:
  build-test-deploy:
    jobs:
      - test-coordinator
      - test-unit
