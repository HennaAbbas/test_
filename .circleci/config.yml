# version: '2.1'
# orbs:
#   slack: circleci/slack@4.1
# jobs:
#   notify:
#     docker:
#       - image: 'cimg/base:stable'
#     steps:
#       - slack/notify:
#           custom: |
#             {
#               "blocks": [
#                 {
#                   "type": "section",
#                   "fields": [
#                     {
#                       "type": "plain_text",
#                       "text": "*This is a text notification*",
#                       "emoji": true
#                     }
#                   ]
#                 }
#               ]
#             }
#           event: always
# workflows:
#   send-notification:
#     jobs:
#       - notify:
#           context: slack-secrets
version: 2
jobs:
  test:
    docker:
      - image: circleci/python:3.9.6
    steps: # steps that comprise the `build` job
      - add_ssh_keys:
          fingerprints:
            - "85:eb:b7:76:37:d7:b7:1f:74:d6:fa:1b:28:ee:b3:73"
      - checkout # check out source code to working directory
      - run : echo $CIRCLE_WORKING_DIRECTORY
      
workflows:
  version: 2
  build:
    jobs:
      - test

