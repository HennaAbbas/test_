version: 2.1
jobs:
  build:
    docker:
      - image: cimg/node
    steps:
      - run: echo $NVM_DIR
      - run: echo "hello world!"

  
      
workflows:
    build_and_push:
      jobs:
        - build
#test
