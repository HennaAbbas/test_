version: 2.1

orbs:
  win: circleci/windows@5.0

jobs:
  build:
    executor: 
      name: win/server-2019
      version: edge
    steps:
      - run:
          name: Query installed components
          command: |
            choco install -y vswhere  # Installs vswhere tool
            vswhere.exe -property components -products *      
    
    
    
    

