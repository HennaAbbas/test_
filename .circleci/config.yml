version: '2.1'

jobs: 
  build:
    docker:
    - image: cimg/python:3.12.2
    working_directory: ~/workdir
    resource_class: small
    steps:
    - setup_remote_docker

workflows:
  my-workflow:
    jobs:
#test
      - build
