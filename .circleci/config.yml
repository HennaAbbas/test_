version: 2.1

#test

  

jobs:
  build:
    docker:
     - image: cimg/node:14.16-browsers
    steps:
      - run: echo "hello world"
          
               

workflows:
  main:
    jobs:
      - build


