version: 2.1

jobs:

  build:
    machine:
      image: ubuntu-2004:202111-01
    steps:
#       - run: circleci update check

      - run:
          name: Check 
          command: |
            if [[ "$CIRCLE_NODE_INDEX" ]]
            then
                echo "run this step"
                echo "the end."
            fi   
      - run: echo "this step should not run"         
workflows:
  build-workflow:
    jobs:
      - build


