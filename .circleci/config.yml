version: 2.1
jobs:
  build:
    docker:
      - image: cimg/base:current
    resource_class: henna/testrunner
    steps:
      - run: echo "Hi I'm on a Container Runner!"
      - run: "please work.  k thanks! "
workflows:
  build-workflow:
    jobs:
      - build