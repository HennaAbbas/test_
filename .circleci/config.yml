version: 2.1

orbs:
  node: circleci/node@4.5.0

jobs:
  fe:
    executor:
      name: node/default
      tag: '16.3.0'

    working_directory: ~/repo

    steps:
      - checkout
      - run: sleep 50000


workflows:
  sqlmonitor:
    jobs:
      - fe

#test
#test
