version: 2.1


# Define the jobs we want to run for this project
jobs:
  build:
    docker:
      - image: cimg/python:3.12.5
    steps:
      - checkout




workflows:
  build_and_test:
    jobs:
      - build: 
             name: build/test
