version: 2.1

  

jobs:
  build:
    docker:
     - image: cimg/node:14.16-browsers
    steps:
      - run: echo "hello world"
          
               

workflows:
  main:
    jobs:
      - build


