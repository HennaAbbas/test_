version: 2.1

jobs:
  test-coordinator:
    type: no-op
  test-unit:
    docker:
      - image: cimg/node:20.10
    steps:
      - checkout
      - run : sleep 1000


workflows:
  build-test-deploy:
    jobs:
      - test-coordinator
      - test-unit: 
          requires: 
            - test-coordinator

#testing
