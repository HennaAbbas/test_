version: 2.1

orbs:
  slack: circleci/slack@4.12.5

workflows:
  my-workflow:
    jobs:
      - build
      - test:
          requires:
            - build
      - deploy:
          requires:
            - test
      # This job runs if any upstream job fails
      - notify-on-failure:
          requires:
            - build: failed
            - test: failed
            - deploy: failed

jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - run: echo "Building..."
      # Uncomment to test failure behavior:
      - run: exit 1
      
  test:
    docker:
      - image: cimg/base:stable
    steps:
      - run: echo "Testing..."
      
  deploy:
    docker:
      - image: cimg/base:stable
    steps:
      - run: echo "Deploying..."
      
  notify-on-failure:
    docker:
      - image: cimg/base:stable
    steps:
      - slack/notify: 
          template: basic_fail_1fail
