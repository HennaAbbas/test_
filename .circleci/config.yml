version: 2.1

jobs:
  build:
    docker:
      - image: cimg/android:2021.9.1
        auth:
          username: henna453  # can specify string literal values
          password: $DOCKERHUB_PASSWORD      
      
    steps:

      - run : printenv 
      - run : echo ${NDK_LTS_VERSION}
      - run : echo ${NDK_STABLE_VERSION}
        

  test:
    docker:
      - image: circleci/python:3.8.12-node-browsers
      
    steps:
      - run : echo "hello world - test job"        
    
            
# # #
workflows:
    build_and_push:
      jobs:
        - build
        - test

#     my-workflow:
#       when:
#         and:
#           - not:
#               matches:
#                 pattern: "^master$"
#                 value: << pipeline.git.branch >>        
#       jobs:
  
#         - test                






