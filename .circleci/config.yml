version: 2.1
jobs:
  build:
    docker: 
      image: cimg/python:3.9-node
    steps:
      - checkout
      - run : echo "hello world"
      - run : sleep 500
workflows:
  workflow:
    jobs:
      - build


#test

