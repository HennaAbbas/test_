version: 2.1

jobs:
  say-hello:

    docker:
      - image: cimg/base:stable

    steps:
      - checkout
      - run: echo $test_var
      - run: echo "You can use the API to set test_env_var=$test_var"
      
  test:

    docker:
      - image: cimg/base:stable

    steps:
      - checkout
      - run: echo $test_var
      - run: echo "You can use the API to set test_env_var=$test_var"      
      
  test_1:

    docker:
      - image: cimg/base:stable

    steps:
      - checkout
      - run: echo $test_var
      - run: echo "You can use the API to set test_env_var=$test_var"      
      

workflows:
  say-hello-workflow:
    jobs:
      - say-hello
  selenium-testing-cron:
     triggers:
       - schedule:
           cron: "0 0 * * *"
           filters:
             branches:
               only:
                 - master

    jobs:
      - test:
          filters:
            branches:
              only:
                - master
      - test_1:
          requires:
            - test             



#test
