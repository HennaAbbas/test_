version: 2.1

jobs:
  say-hello:

    docker:
      - image: cimg/base:stable

    steps:
      - checkout
      - run: echo $test_var
      - run: echo "You can use the API to set test_env_var=$test_var"

workflows:
  say-hello-workflow:
    jobs:
      - say-hello: 
          context: 
             my_context



