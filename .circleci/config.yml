version: 2.1

orbs:
  node: circleci/node@4.5.0

jobs:
  stress-frontend-e2e-test:
    machine:
      docker_layer_caching: false
      image: ubuntu-2204:current

    steps:
      - checkout
      - run : sleep 5000   


workflows:
  sqlmonitor:
    jobs:
      - stress-frontend-e2e-test:  
          type: approval
