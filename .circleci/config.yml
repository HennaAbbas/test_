version: 2.1


jobs:
    
   deploy:
      
    machine: 
        image: ubuntu-2004:202201-02
    steps:
      - checkout
      - run: pwd
      - run: ls -al
      - run: docker build - < .Dockerfile





workflows:
  my_workflow:
    jobs:
        - deploy

      


#test 

