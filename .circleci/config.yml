version: 2.1

jobs:
  build_and_publish_image:
    docker:
      - image: cimg/python:3.9.7

    working_directory: ~/project
    steps:
      - checkout



workflows:
  build:
    jobs:
      - build_and_publish_image:
          filters:
            branches:
              only: docker


#test
