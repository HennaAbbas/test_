version: 2.1


jobs:
  build:
    docker:
      - image: cimg/python:3.11.0-node
    steps:
      - run : sleep 5000
      - run: echo "OK" 
      - when:
          condition: 
            or:
              - and:
                - equl: [ api, << pipeline.trigger_source >> ]
              - equl: [ webhook, << pipeline.trigger_source >> ]
            steps:
              - run: echo "this sould not ALWAY trigger"
                   

workflows:
  build:
    jobs:
      - build

