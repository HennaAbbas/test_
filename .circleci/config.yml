version: 2
jobs:
  test:
    docker:
      - image: cimg/python:3.9.10-browsers
    steps: # steps that comprise the `build` job
     - run: echo "hello world!"      

        
      
workflows:
  version: 2
  build:
    jobs:
      - test
