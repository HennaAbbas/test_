version: 2.1


  

jobs:
  build:
    docker:
     - image: cimg/node:14.16-browsers
    steps:
      - run: echo "hello world"
          
          
  test:
    machine:
      image: ubuntu-2004:202201-02
    resource_class: medium

    steps:
      - checkout
  



workflows:
  main:
    jobs:
      - build
      - test

          
