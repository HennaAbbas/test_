version: 2.1
jobs:
  build:
    docker:
      - image: cimg/node:18.0.0
    steps:
      - run: echo $NVM_DIR
      - run: echo "hello world!"

  
      
workflows:
    build_and_push:
      jobs:
        - build
#test
