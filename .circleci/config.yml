version: 2.1

orbs:
  win: circleci/windows@5.0

jobs:
  build:
    executor: 
      name: win/server-2019
      version: edge
    steps:
      - run:
          name: Install vstest
          command: choco install visualstudio2017testagent -y
      - run:
          name: List Chocolatey packages
          command: choco list 

          
    
    
    

