version: 2.1
jobs:
  build:
    machine: true
    resource_class:  henna/test_runner_2
    steps:
      - run: echo "Hi I'm on a Container Runner"
      - run: echo "please work.  k thanks"
      - run: sleep 500
workflows:
  build-workflow:
    jobs:
      - build
     

