version: 2.1
orbs:
  node: circleci/node@5.0.2
jobs:
  build-web:
    machine:
      image: default
    resource_class: large
    steps:
      - node/install:
          node-version: '22.1.0'
workflows:
  my_workflow:

    jobs:
      - build-web    


