version: 2.1

workflows:
  my-workflow:
    jobs:
      - run-tests
      - second_job   
   
  
jobs:
  run-tests:
    macos:
      xcode: 11.3.0
    steps:

      - run: echo "hello world"
      - run: npm list -g
      - run: npm -v   
      - run: sudo npm install -g appium-doctor
      - run: sudo npm install -g appium
      - run: npm view appium version
     
          
  second_job:
    docker:
      - image: cimg/base:2020.01
    steps:
      - run: echo "bye world. This is a scheduled workflow/job"



