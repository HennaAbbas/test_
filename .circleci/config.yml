version: '2.1'

jobs:
  build:
#     docker:
#     - image: windows-server-2022-gui:edge
    machine:
      image: windows-server-2022-gui:edge

    steps:
      - checkout


workflows:
  main:
    jobs:
      - build


