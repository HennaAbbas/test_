version: 2
jobs:
  test:
    docker:
      - image: cimg/ruby:3.1
    steps: # steps that comprise the `build` job
     - run: echo "hello world!"      
      
workflows:
  version: 2
  build:
    jobs:
      - test
