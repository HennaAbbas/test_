
version: '2.1'

jobs:
  build:
    resource_class: medium
    docker:
      - image: $image
    steps:
      - checkout
 
      

workflows:
  main:
    jobs:
      - build: 
          context: 
            -my-context
         

