version: '2.1'
orbs:
  win: circleci/windows@4.1
jobs:
  build:
    executor:
      name: win/default
    steps:
      - run: python --version
workflows:
  my-workflow:
    jobs:
      - build

          
