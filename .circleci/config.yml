
   
version: 2.1

jobs:
  build:
    docker:
      - image: circleci/python:3.9

    steps:
      - run : python3 -m pip install coverage
      - run : echo $PATH 
  docs-build-deploy:
    docker:
      - image: circleci/python:3.9.6
        environment: # environment variables for primary container
          PIPENV_VENV_IN_PROJECT: true
    steps:
      - add_ssh_keys:
          fingerprints:
            - "83:54:85:e4:8f:45:9f:af:91:92:29:48:51:33:47:cb"      

            
# # #
workflows:
    build_and_push:
      jobs:
        - build 
        - docs-build-deploy
       
