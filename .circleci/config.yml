version: 2.1

orbs:
  browser-tools: circleci/browser-tools@1.1.1

jobs:
  build:
    machine: true
    resource_class:  henna/test_runner_3
    steps:
      - checkout
      - browser-tools/install-chrome

workflows:
  build-workflow:
    jobs:
      - build
